{% extends 'base.html.twig' %}

{% block title %}Jugement{% endblock %}

{% block body %}
    <h1>Jugement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jugement.id }}</td>
            </tr>
            <tr>
                <th>Jugements</th>
                <td>{{ jugement.jugements }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_jugement_index') }}">back to list</a>

    <a href="{{ path('app_jugement_edit', {'id': jugement.id}) }}">edit</a>

    {{ include('jugement/_delete_form.html.twig') }}
{% endblock %}
