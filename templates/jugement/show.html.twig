{% extends 'base.html.twig' %}

{% block title %}Jugement{% endblock %}

{% block body %}
<style>
.back{
            color:#aa80ff;
        }
         .back:hover{
            color:#884dff;
            text-decoration:underline;
            background-color:#eee6ff;

        }
</style>
    <h1>Jugement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jugement.id }}</td>
            </tr>
            <tr>
                <th>Jugements</th>
                <td>{{ jugement.jugements }}</td>
            </tr>
        </tbody>
    </table>

    <a class="back" href="{{ path('app_jugement_index') }}">back to list</a>

    <a class="back" href="{{ path('app_jugement_edit', {'id': jugement.id}) }}">edit</a>

    {{ include('jugement/_delete_form.html.twig') }}
{% endblock %}
